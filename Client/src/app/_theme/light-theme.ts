import { Theme } from './symbols';

export const lightTheme: Theme = {
  name: 'light',
  properties: {
    '--background': '#E5E5E5',
    '--on-background': '#000',
    '--primary': '#FFFFFF',
    '--on-primary': '#000'
  }
};
